version: 3
agent:
  authtoken: $NGROK_AUTHTOKEN
endpoints:
  - name: delta_app
    url: $NGROK_URL
    upstream:
      url: ${LOCAL_APP_URL}

  - name: delta_ws
    url: ${NGROK_WS_URL}
    upstream:
      url: http://reverb:8080
